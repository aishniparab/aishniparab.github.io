<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://aishniparab.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://aishniparab.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2025-10-08T17:28:20+00:00</updated><id>https://aishniparab.github.io/feed.xml</id><title type="html">Aishni Parab</title><subtitle></subtitle><entry><title type="html">Genetic Programming: A Conceptual Crash Course</title><link href="https://aishniparab.github.io/blog/2025/genetic-algos/" rel="alternate" type="text/html" title="Genetic Programming: A Conceptual Crash Course"/><published>2025-02-11T00:00:00+00:00</published><updated>2025-02-11T00:00:00+00:00</updated><id>https://aishniparab.github.io/blog/2025/genetic-algos</id><content type="html" xml:base="https://aishniparab.github.io/blog/2025/genetic-algos/"><![CDATA[<p>Genetic programming (GP) is a computational technique that originated in the 1960s <d-cite key="forrest1996genetic"></d-cite>. It evolves programs through a process similar to natural selection, optimizing for a predefined fitness function. The goal of GP is to create programs that approximate a target function. In the example shown, the target function to approximate is ( y = x^2 ).</p> <h2 id="key-steps-in-genetic-programming">Key Steps in Genetic Programming:</h2> <ol> <li><strong>Generate Random Initial Programs</strong>: The process begins with randomly generating programs (solutions).</li> <li><strong>Evaluate Fitness</strong>: The fitness of each program is evaluated by comparing its output to the expected values of the target function.</li> <li><strong>Apply Mutation and Crossover</strong>: New programs are created by mutating existing ones or combining parts of two programs through crossover.</li> <li><strong>Repeat the Process</strong>: This cycle is repeated iteratively until a satisfactory program emerges that approximates the target function with minimal error.</li> </ol> <h2 id="example">Example:</h2> <p>The specific task is to evolve a function to approximate ( y = x^2 ) where ( x ) takes values from the set { -2, -1, 0, 1, 2 }, and the corresponding expected outputs ( y ) are { 4, 1, 0, 1, 4 }.</p> <ul> <li><strong>Terminals</strong>: y, x, 1, 2, …</li> <li><strong>Operators</strong>: ( +, -, \times, /, \% )</li> <li> <p><strong>Fitness Function</strong>: The fitness of a program is calculated using the formula:</p> <p>[ \text{Fitness} = \sum_{i=1}^{n} (y_{\text{output}} - y_{\text{expected}})^2 ]</p> </li> </ul> <p>Where ( y_{\text{output}} ) is the program’s output, and ( y_{\text{expected}} ) is the target output.</p> <h2 id="process-example">Process Example:</h2> <p><strong>Initial Programs</strong>:</p> <ol> <li>( P1 ) generates: ( y = x + 1 ) with fitness = 20</li> <li>( P2 ) generates: ( y = x^2 ) with fitness = 16</li> <li>( P3 ) generates: ( y = x + x ) with fitness = 16</li> </ol> <p><strong>Selection</strong>: Select ( P2 ) and ( P3 ) for crossover as they are the most fit.</p> <p><strong>Crossover</strong>: Combining parts of ( P2 ) and ( P3 ), we produce the new program ( P4 ), which is ( y = x \times x ) (fitness = 0).</p> <p><strong>Mutation</strong>: Further mutation of the program can refine it. Here, replacing part of ( P3 ) produces program ( P5 ), which also converges to the desired result.</p> <p><strong>Final Convergence</strong>: Both ( P4 ) and ( P5 ) converge to the target output with a fitness of 0, meaning they perfectly approximate the function ( y = x^2 ).</p> <p>This process highlights the power of genetic programming in evolving increasingly efficient solutions through iterative selection, mutation, and crossover processes.</p>]]></content><author><name></name></author><category term="evolutionary-algorithms"/><category term="search"/><summary type="html"><![CDATA[Genetic programming (GP) is a computational technique that originated in the 1960s . It evolves programs through a process similar to natural selection, optimizing for a predefined fitness function. The goal of GP is to create programs that approximate a target function. In the example shown, the target function to approximate is ( y=x^2 ).]]></summary></entry><entry><title type="html">Mathematical discoveries from program search with large language models: A Summary</title><link href="https://aishniparab.github.io/blog/2025/funsearch/" rel="alternate" type="text/html" title="Mathematical discoveries from program search with large language models: A Summary"/><published>2025-02-11T00:00:00+00:00</published><updated>2025-02-11T00:00:00+00:00</updated><id>https://aishniparab.github.io/blog/2025/funsearch</id><content type="html" xml:base="https://aishniparab.github.io/blog/2025/funsearch/"><![CDATA[<p>A recent paper, <em>Mathematical Discoveries from Program Search with Large Language Models</em><d-cite key="romera2024mathematical"></d-cite>, introduces <strong>FunSearch</strong>, a novel approach that redefines how we solve complex mathematical and computational problems. Instead of searching for solutions directly, <strong>FunSearch searches for functions that describe how to solve a problem</strong>. These functions are essentially programs that encode the structure and logic of the underlying problem. Rather than merely presenting a result, these programs articulate the logical process behind the solution. By evaluating a program across a range of inputs, its effectiveness in solving the problem can be assessed.</p> <p>Large language models (LLMs) have demonstrated remarkable proficiency in generating code, making them well-suited for producing such programs. However, they suffer from <strong>hallucinations</strong>—producing incorrect or unverifiable outputs. LLMs also struggle to move beyond known examples, often generating solutions that resemble patterns from their training data rather than discovering genuinely new insights.The FunSearch paper demonstrated that <strong>combining LLMs with evolutionary algorithms</strong> can overcome these limitations, systematically explore vast function spaces, and achieve groundbreaking discoveries in two NP-hard problems: the Cap Set problem and Online Bin Packing.</p> <p>FunSearch depends on a few key aspects:</p> <ol> <li><strong>Pre-trained (frozen) LLM</strong>: Codey, an LLM built on top of the PaLM2 model family, fine-tuned on a large corpous of code is used to sample a program.</li> <li><strong>Evaluation</strong>: The program is evaluated symbolically on a range of inputs and the score of the program is computed for various inputs, and then aggregated using the mean function (see appendix for score function). The scored programs are then stored in the program database. Incorrect programs or programs that timed-out within imposed time and memory limits are discarded.</li> <li><strong>Program database</strong>: A database is used to maintain a diverse set of correct programs. To maintain diversity and prevent convergence to local optima, an island model is used, where multiple subpopulations evolve independently with occasional migration. The lowest-performing half of the islands are periodically reset by replacing their populations with clones of top individuals from the surviving islands.</li> <li><strong>Best-shot prompting:</strong> <em>k</em> programs are sampled from the database by first selecting an island, then choosing a program within it, favoring higher-scoring and shorter ones. The sampled programs, sorted by score, are assigned version labels (e.g., v_0 for the lowest-scoring, v_1 for the second lowest, etc.). These programs are then combined into a single prompt, with the version appended as a suffix to the function name (e.g., function_v0). In practice, k = 2 is used, as higher values yield diminishing returns. This structured prompt is then fed to the LLM to generate a new program.</li> <li><strong>Skeleton-based initialization:</strong> The initial program contains boilerplate code and predefined problem structure while leaving the core logic open for evolution.</li> </ol> <p>The figure below illustrates these key components.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/funsearch_pipeline-480.webp 480w,/assets/img/funsearch_pipeline-800.webp 800w,/assets/img/funsearch_pipeline-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/funsearch_pipeline.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>FunSearch operates in a <strong>distributed manner</strong>, with three key components communicating asynchronously:</p> <ul> <li><strong>Programs Database:</strong> Stores and serves candidate programs.</li> <li><strong>Samplers:</strong> Generate new functions using the pretrained LLM.</li> <li><strong>Evaluators:</strong> Assess the effectiveness of programs.</li> </ul> <p>Since LLM sampling and evaluation occur concurrently, FunSearch can scale by adding more samplers or evaluators. The experiments conducted in this paper used <strong>15 samplers and 150 CPU evaluators</strong> (5 CPU servers each running 32 evaluators), all running in parallel. Since there is inherent randomness in the system architecture, the experiments were repeated multiple times.</p> <h2 id="applying-funsearch-to-np-hard-problems">Applying FunSearch to NP-Hard Problems</h2> <p>The authors of this paper demonstrate the power of FunSearch by applying it to two NP-hard problems, the Cap Set problem and the Online Bin Packing problem.</p> <p>The Cap Set Problem is a question in extremal combinatorics that asks how large a set of numbers can be while avoiding any three that form a simple pattern like an arithmetic progression (a sequence of numbers where the difference between consecutive terms is the same, such as 2, 4, 6 or 3, 6, 9). A simple example is picking numbers from {1, 2, 3, 4, 5, 6, 7} while avoiding any three that are evenly spaced, such as {1, 3, 5} or {2, 4, 6}. While this example is in one dimension, the Cap Set Problem generalizes to higher-dimensional spaces, where numbers are replaced by vectors in an n-dimensional space over a finite field, making the problem significantly more complex.</p> <p>The Online Bin Packing Problem is a classic optimization problem in theoretical computer science and operations research, where items of varying sizes arrive sequentially and must be packed into bins of fixed capacity without knowledge of future items. The goal is to minimize the number of bins used while making irrevocable placement decisions as each item arrives. A simple example is packing numbers representing item sizes, such as {4, 7, 2, 5, 6}, into bins of size 10. If the items arrive one by one, an online strategy might place 4 and 7 in separate bins before realizing later that 4 and 6 could have fit together, demonstrating the challenge of not knowing the full sequence in advance.</p> <p>The figure below shows the specification for the Cap Set problem (a) and the Online Bin packing problem (b).</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/funsearch_spec-480.webp 480w,/assets/img/funsearch_spec-800.webp 800w,/assets/img/funsearch_spec-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/funsearch_spec.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>The spec contains the evaluate function which takes in the solution and returns a score assessing it, the solve function contains the skeleton algorithm, which calls the function to evolve that contains the crutial logic. In Cap Set problem, priority function is the function to be evolved and in Online Bin Packing, the heuristic function is the fucntion to be evolved. The main function uses the solve function to solve the problem, then uses the evaluate function to score the solution. Sometimes, as shown in b, the main function can implement additional logic. This format allows FunSearch to focus on the most critical part of the program, avoiding failures due to mistakes in parts of the program that are already known. Additional information about the known parts of the problem can be provided in the form of docstrings, relevant primitive functions or import packages.</p> <p>The specification consists of three key functions: <strong>evaluate</strong>, which scores a given solution; <strong>solve</strong>, which serves as the skeleton algorithm; and a function to be evolved, which encapsulates the critical decision-making logic. In the <strong>Cap Set problem</strong>, the <strong>priority</strong> function is evolved, while in <strong>Online Bin Packing</strong>, the <strong>heuristic</strong> function is evolved. The <strong>main</strong> function orchestrates the solution by calling <strong>solve</strong> and then using <strong>evaluate</strong> to assess its quality. In some cases, as seen in the Online Bin Packing example, additional logic is implemented within the main function. This modular structure allows <strong>FunSearch</strong> to focus on evolving the most crucial aspects of the program while leveraging known and well-defined components. Supporting information, such as docstrings, relevant utilities, or predefined imports, can al so be provided.</p> <p>For the <strong>cap set</strong> problem, the experiment involved evolving a function that prioritizes which vectors to include in a set, ensuring no three sum to zero. Using this approach, the method discovered a larger cap set in dimension 8 than previously known and improved the lower bound on cap set capacity, demonstrating its ability to scale to higher dimensions and generate novel mathematical insights.</p> <p>For the <strong>bin packing</strong> problem, the experiment evolved a heuristic that decides where to place incoming items in bins to minimize wasted space. The discovered heuristic outperformed traditional methods (first fit and best fit) across multiple datasets, generalizing well to larger problem instances and improving efficiency in practical scenarios.</p> <p>The specification consists of three key functions: <strong>evaluate</strong>, which scores a given solution; <strong>solve</strong>, which serves as the skeleton algorithm; and a function to be evolved, which encapsulates the critical decision-making logic. In the <strong>Cap Set problem</strong>, the <strong>priority</strong> function is evolved, while in <strong>Online Bin Packing</strong>, the <strong>heuristic</strong> function is evolved. The <strong>main</strong> function orchestrates the solution by calling <strong>solve</strong> and then using <strong>evaluate</strong> to assess its quality. In some cases, as seen in the Online Bin Packing example, additional logic is implemented within the main function. This modular structure allows <strong>FunSearch</strong> to focus on evolving the most crucial aspects of the program while leveraging known and well-defined components. Supporting information, such as docstrings, relevant utilities, or predefined imports, helps ensure clarity and correctness.</p> <h2 id="key-takeaways">Key Takeaways</h2> <ul> <li><strong>Searching in function space enables more efficient discovery</strong>: Instead of directly searching for solutions—such as an enormous list of numbers in the Cap Set problem—FunSearch searches for functions (expressed as programs) that describe how to solve the problem. These functions provide a more structured and interpretable representation.</li> <li><strong>Programs offer an interpretable and concise representation</strong>: Many problems of interest are highly structured rather than random. Solutions can often be described more concisely with a computer program than with other representations. For instance, the trivial representation of the admissible set A(24, 17) consists of over 200,000 vectors, whereas the program generating this set is only a few lines of code.</li> <li><strong>Kolmogorov-compressed inductive bias enables scalability</strong>: FunSearch <strong>favors concise programs</strong>, allowing it to scale to much larger problem instances compared to traditional search methods. In a loose sense, FunSearch <strong>optimizes for low Kolmogorov complexity</strong>—the shortest program that produces a given output—whereas conventional search algorithms follow a different inductive bias.</li> <li><strong>LLMs should be viewed as program generators, not problem solvers</strong>: The pre-trained LLM does not rely heavily on problem-specific context. Instead, it acts as a generator of diverse, syntactically correct programs, occasionally producing novel and useful ideas.</li> <li><strong>LLMs have a natural bias for human-written code</strong>: Unlike genetic algorithms, which evolve solutions without any preference for human readability, LLMs inherently favor structured, human-like code, making their outputs more interpretable and aligned with conventional programming practices.</li> </ul> <h2 id="when-is-funsearch-applicable">When is FunSearch Applicable?</h2> <p>FunSearch is most effective for problems that meet the following criteria:</p> <ul> <li><strong>Well-defined evaluator</strong>: The problem must have a clear evaluation function that can assess the quality of a solution.</li> <li><strong>Rich scoring feedback</strong>: The evaluation should provide <strong>granular, informative feedback</strong> to guide iterative improvements.</li> <li><strong>Structured skeleton with an isolated evolvable component</strong>: The problem should allow for a predefined program structure (skeleton) while leaving a critical decision-making function open for evolution.</li> </ul> <p>Problems like <strong>MAX-SAT</strong> are well-suited for FunSearch because the number of <strong>satisfied clauses</strong> naturally serves as a <strong>scoring signal</strong>. In contrast, theorem proving is more challenging to frame within this paradigm, as it lacks a straightforward scoring function that can provide meaningful intermediate feedback.</p>]]></content><author><name></name></author><category term="evolutionary-algorithms"/><category term="search"/><category term="article-notes"/><summary type="html"><![CDATA[A recent paper, Mathematical Discoveries from Program Search with Large Language Models, introduces FunSearch, a novel approach that redefines how we solve complex mathematical and computational problems. Instead of searching for solutions directly, FunSearch searches for functions that describe how to solve a problem. These functions are essentially programs that encode the structure and logic of the underlying problem. Rather than merely presenting a result, these programs articulate the logical process behind the solution. By evaluating a program across a range of inputs, its effectiveness in solving the problem can be assessed.]]></summary></entry><entry><title type="html">The Wonder of Life No. 1: Structure and Emergence</title><link href="https://aishniparab.github.io/blog/2025/wonder-of-life/" rel="alternate" type="text/html" title="The Wonder of Life No. 1: Structure and Emergence"/><published>2025-02-11T00:00:00+00:00</published><updated>2025-02-11T00:00:00+00:00</updated><id>https://aishniparab.github.io/blog/2025/wonder-of-life</id><content type="html" xml:base="https://aishniparab.github.io/blog/2025/wonder-of-life/"><![CDATA[<p>Life operates within constraints, shaped by the structured nature of the universe, yet it finds remarkable ways to thrive through randomness and adaptation. Agriculture is a prime example of this delicate balance—farmers must navigate an ever-changing environment, adjusting to shifting weather patterns, soil conditions, and unpredictable challenges in an effort to secure high crop yields. When a lab at MIT offered them a controlled environment with perfect conditions, the response was unexpected. It turns out that much of agricultural evolution has been shaped by the constraints of daily cycles— the sun rising and setting, seasons changing, and natural fluctuations in climate. Plants, over time, have evolved within these boundaries, adapting their growth, metabolism, and even dormancy patterns to fit this rhythm.</p> <p>Risto Mikkulainen, in collaboration with the MIT Media Lab, conducted an experiment to test what happens when these constraints are removed <d-cite key="johnson2019flavor"></d-cite>. By simulating perfect conditions for a basil plant—continuous light, ideal nutrients, and optimized growth parameters—they found that the plant did not need to “sleep” at all. Not only did it grow larger, but it also developed a richer taste. This suggests that some biological limits are not intrinsic necessities but rather evolutionary compromises made in response to the external world.</p> <p>This raises a profound question: how much of what we consider “natural” is simply an adaptation to external constraints rather than an inherent requirement for life? The structured universe, with its laws and patterns, creates the boundaries within which randomness and adaptation occur. Evolution does not take place in an arbitrary void but within a fixed structure that guides its possibilities. The interplay between necessity and chance leads to fascinating emergent properties—traits that would never appear without the tension between order and randomness.</p> <p>The wonder of life lies in this duality: it conforms to structure, yet within those constraints, it explores novel paths. Evolution is not just survival within boundaries; it is an ongoing experiment of what is possible when structure meets chance. Perhaps, by altering the constraints—whether in agriculture, biology, or human life—we might uncover new potentials, revealing possibilities that nature never had the chance to explore.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/tree-of-life-darwin-480.webp 480w,/assets/img/tree-of-life-darwin-800.webp 800w,/assets/img/tree-of-life-darwin-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/tree-of-life-darwin.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>Charles Darwin’s 1837 Tree of Life sketch from his First Notebook on Transmutation of Species, <a href="https://www.cam.ac.uk/stories/darwins-tree-of-life">mysteriously stolen from Cambridge Library in 2000 and anonymously returned years later</a>.</p>]]></content><author><name></name></author><category term="evolution"/><category term="agriculture"/><category term="wonder-of-life-series"/><summary type="html"><![CDATA[Life operates within constraints, shaped by the structured nature of the universe, yet it finds remarkable ways to thrive through randomness and adaptation. Agriculture is a prime example of this delicate balance—farmers must navigate an ever-changing environment, adjusting to shifting weather patterns, soil conditions, and unpredictable challenges in an effort to secure high crop yields. When a lab at MIT offered them a controlled environment with perfect conditions, the response was unexpected. It turns out that much of agricultural evolution has been shaped by the constraints of daily cycles— the sun rising and setting, seasons changing, and natural fluctuations in climate. Plants, over time, have evolved within these boundaries, adapting their growth, metabolism, and even dormancy patterns to fit this rhythm.]]></summary></entry><entry><title type="html">Core Design Principles of Neuro-Symbolic Models</title><link href="https://aishniparab.github.io/blog/2025/principles-of-ns-models/" rel="alternate" type="text/html" title="Core Design Principles of Neuro-Symbolic Models"/><published>2025-02-01T00:00:00+00:00</published><updated>2025-02-01T00:00:00+00:00</updated><id>https://aishniparab.github.io/blog/2025/principles-of-ns-models</id><content type="html" xml:base="https://aishniparab.github.io/blog/2025/principles-of-ns-models/"><![CDATA[<p>Neurosymbolic models operate within a design space centered on user intent, program generation, and task execution. User intent serves as the guiding principle, describing the task to be performed and specifying the formal language in which the output program should be expressed. These models include a mechanism to generate symbolic programs that align with this intent. Once generated, these programs are executed to produce data that fulfills the specified task. Importantly, the generated output may undergo further refinement or enhancement through a non-symbolic (neural) post-processing step.</p> <h2 id="task-specification">Task Specification</h2> <h3 id="inputs-and-outputs">Inputs and Outputs</h3> <p>Typically, the input to a neuro-symbolic model can be a single visual entity, such as an image or 3D shape, to be reconstructed, or a collection of visual data with the goal of emulating its distribution. Additionally, inputs can encompass a black-box visual data generator whose behavior should be emulated, a natural language text prompt describing the desired type of visual data to generate, or objective functions that map a program’s visual output to a desirability score. Notably, these latter possibilities remain largely unexplored <d-cite key="ritchie2023neurosymbolic"></d-cite>.</p> <p>Consequently, a neuro-symbolic model can produce various types of outputs, including deterministic programs, probabilistic programs, or program distributions. A deterministic program generates a fixed visual output each time it is executed, while allowing its parameters to be adjusted to produce variations in the output (e.g., CAD programs). A probabilistic program, in contrast, encapsulates a generative model, enabling it to produce diverse visual outputs by sampling from its internal probabilistic structure during execution. A program distribution takes this a step further by generating a variety of programs themselves, each of which may independently produce outputs. This additional layer of abstraction allows the system to explore a broader space of possible solutions by sampling diverse programs, rather than being confined to variations within a single program.</p> <h2 id="program-specification">Program Specification</h2> <p>To enable a neurosymbolic model to generate programs, a specification language is essential for defining how these programs should be expressed. While general-purpose programming languages like Python or C++ are options, domain-specific languages (DSLs) are often preferred due to their ability to provide specialized operators and data types tailored to a particular domain. This specialization makes DSLs more concise, easier to modify, and reduces the search space of possible outputs, enhancing efficiency. The design of a DSL involves key considerations, including the programming paradigm, primitive types, and mutability. The choice of programming paradigm shapes how the language operates: functional paradigms prioritize immutability and declarative expressions, imperative paradigms center on sequential instructions and state changes, and constraint paradigms define relationships resolved by solvers. Primitive elements in a DSL can either be manually crafted or augmented with machine learning through neural primitives, which are data types or functions represented by neural networks. Mutability is another crucial aspect of DSL design. A language can remain fixed, undergo preprocessing modifications to identify and factor out common patterns into new procedures, or evolve as the model learns. In some cases, no predefined language is provided, allowing the neurosymbolic model to invent its own. In this case, the model must propose a set of primitive data types and functions capable of combining these types to effectively represent visual data. This approach, where the model invents its own language, remains an intriguing and largely unexplored area.</p> <h2 id="program-synthesizer">Program Synthesizer</h2> <p>With a given task specification and a DSL, a neurosymbolic model must generate programs that fulfill the requirements of the specification within the constraints of the DSL. This process is essentially a <strong>search problem</strong>, involving the exploration of the program space defined by the DSL to identify those that meet the specification.</p> <p><strong>Prior:</strong> It is important to define <em>a priori</em> the preferred characteristics of programs, as any DSL will likely produce multiple programs that satisfy the input specification. Consistent with Occam’s razor, existing research generally favors shorter, simpler programs.</p> <p>Various <strong>search algorithms</strong> have been applied to program synthesis, each with distinct strategies and use cases.</p> <p><strong>Database Retrieval:</strong> The simplest approach is database retrieval, where instead of generating a new program, the method retrieves an existing one from a database and adjusts its parameters to better meet user goals.</p> <p><strong>Enumerative Search:</strong> Explicit enumeration, another approach, can be implemented either top-down or bottom-up, exploring the program space from the root or the leaves of an abstract syntax tree (AST). However, this method is practical only for identifying short programs in simple languages, as the complexity of larger spaces requires additional techniques to remain computationally feasible. In complex scenarios, additional techniques are necessary to efficiently navigate the expansive search space for programs. Heuristics are commonly used to reduce the search space, with greedy search serving as the most restrictive approach by prioritizing immediate results. Beam search and its variations are widely applied to achieve a balance between thorough exploration and computational efficiency. Top-down search methods, such as branch-and-bound, provide another effective strategy by systematically pruning less optimal paths. More recent innovations have focused on compressing the search space by compactly representing large sets of equivalent sub-programs, leveraging tools like version spaces [[^1]] and e-graphs [[^2].</p> <p><strong>Stochastic Search</strong>: When the program space defined by a language becomes too vast for enumeration to be practical, randomized search methods provide a viable alternative. These approaches typically begin with an initial set of programs, which may be generated randomly or guided by heuristics, and iteratively apply random modifications to improve the programs. Techniques like Markov Chain Monte Carlo (MCMC) and simulated annealing operate on a single candidate program at a time, refining it through stochastic updates. In contrast, algorithms such as Sequential Monte Carlo (SMC) and genetic programming maintain a population of programs, applying random changes across multiple candidates simultaneously.</p> <p><strong>Constraint Satisfaction</strong>: Some tasks can be addressed by converting the specification into a set of constraints, treating the problem as one of constraint satisfaction. A common approach involves translating the specification into a Boolean satisfiability (SAT) problem, enabling the use of efficient SAT solvers, provided the problem size remains manageable. If direct conversion to SAT is infeasible, the specification can instead be expressed as a first-order logic satisfiability problem, allowing the application of SMT solvers.</p> <p><strong>User-in-the-loop:</strong> Instead of automatically synthesizing a program, a user can provide suggestions about what different parts of the output program should look like, helping the synthesizer make decisions.</p> <p><strong>Termination</strong>: Enumeration-based and constraint-based algorithms terminate once all possible programs have been explored, but this can demand an intractable amount of computation. In contrast, stochastic search algorithms lack a natural termination point and can, in principle, run indefinitely, potentially producing progressively better programs over time. As a result, program synthesizers often treat these search methods as anytime algorithms, designed to return the best program discovered within a predefined computational budget. This approach ensures useful results even when the search cannot fully exhaust the program space.</p> <p><strong>Neural Guidance</strong>: Many search algorithms benefit from heuristic guidance, which is where machine learning often plays a role. For example, beam search requires ranking possible next states, branch-and-bound needs to estimate bounds on scores before expanding, and stochastic methods can use non-uniform distributions for random changes. Neural guidance typically involves learning heuristics to suggest what to add to a program next. Autoregressive language models, especially transformers, are widely used for this purpose, while earlier methods employed pointer networks to suggest variable re-use or graph convolutional networks, which have become less common since the rise of attention-based models. In program retrieval systems, deep feature spaces for similarity search also act as neural guidance. Some approaches bypass search entirely, generating programs in a single forward pass of a neural network. These methods can be categorized as either explicit enumeration or stochastic search with neural guidance, depending on whether the network selects the most probable choice or samples randomly.</p> <h2 id="program-execution">Program Execution</h2> <p>Once a program is generated, it has to be executed to produce an output. This execution process can be implemented in various ways. The most common approach is to directly execute the program. Alternatively, some languages require solving a search, optimization or a constraint-satisfying problem. This approach typically increases learning complexity as computing gradients with respect to an execution engine may not be straightforward. Here, a neural network can be used to guide search during program execution. While we can use hardcoded execution engines, it is possible to use a learned module which maps program tokens to program output. Although this learned mapping is only an approximation it can aid learning of other neural components.</p> <h2 id="neural-postprocessing">Neural Postprocessing</h2> <p>After executing the program, one can include a post-processing component that refines the output to make it more realistic, e.g. improve the quality of the image.</p> <h2 id="learning-algorithm">Learning Algorithm</h2> <p>A neurosymbolic model can be trained using one of two approaches: end-to-end in an unsupervised manner or modularly in a supervised fashion. In end-to-end training, the final task loss is backpropagated through all components of the model, including each decision made by the synthesizer in constructing the program. However, programs are inherently discrete due to branching factors and primitive types, making it difficult to define gradients over discrete choices.</p> <p>To address this challenge, one approach is to introduce a continuous relaxation of discrete program choices. This requires defining a program executor capable of producing semantically meaningful programs. The design of such relaxations and their executors is highly domain-specific and demands expert knowledge. Nevertheless, once a continuous space over programs is established, the system becomes end-to-end differentiable.</p> <p>An alternative strategy is to employ policy gradient reinforcement learning using a score function gradient estimator. This method does not require any component of the model to be differentiable, as it stochastically estimates gradients in an unbiased manner. However, these algorithms often suffer from slow convergence, frequently getting trapped in poor local optima or failing to converge at all due to high variance.</p> <p>A neurosymbolic model can also be trained in a modular fashion, where each module is supervised using input-output pairs specific to that module. However, obtaining input data paired with ground-truth program annotations is often challenging. In practice, human-written programs satisfying particular task specifications have been used, but such data is difficult to acquire. Thus, an alternative approach involves generating synthetic programs and task specification pairs, for example, by randomly deriving programs from a domain-specific language (DSL) grammar. While this method allows for scalable data generation, synthetic data does not generalize well due to its limited complexity and diversity compared to real-world programs. As a result, synthetic datasets are primarily used for initialization rather than as a primary learning method. A common approach to mitigate this limitation is bootstrapping, where a model is first pre-trained on synthetic data and then refined iteratively by training on variations of its own predictions.</p> <p>[^1] <strong>Definition of a version space.</strong> A <strong>version space</strong> is a conceptual framework used to represent all hypotheses consistent with a set of observations or constraints. In the context of program synthesis, a version space is a compact representation of all programs that meet a given specification. Instead of enumerating each individual program, the version space describes a set of equivalent or partially equivalent programs, often using symbolic or parametric constraints. This approach helps reduce the computational burden by focusing on a concise yet comprehensive representation of viable solutions. For example, in machine learning, a version space is often defined as the set of hypotheses that remain consistent with the training data. In program synthesis, it could mean all abstract syntax trees (ASTs) that can perform a desired task within a specific domain or under certain constraints.</p> <p>Version spaces focus on the representation of all consistent programs with a specification but do not inherently store equivalence relationships.</p> <p>For example, suppose you are synthesizing a program to compute the area of a rectangle. The specification requires that the program take two inputs, length and width, and output their product. A version space would represent all possible programs that satisfy this specification, like this:</p> <ul> <li><code class="language-plaintext highlighter-rouge">Hypothesis 1: area = length * width</code></li> <li><code class="language-plaintext highlighter-rouge">Hypothesis 2: area = width * length (commutativity)</code></li> <li><code class="language-plaintext highlighter-rouge">Hypothesis 3: area = sum(length repeated width times) (alternative algorithm)</code></li> </ul> <p>[^2] <strong>Definition of an e-graph</strong>. An <strong>e-graph</strong> is a data structure used to compactly represent equivalence classes of expressions. Each node in an e-graph represents an <strong>equivalence class</strong>—a set of expressions that are considered semantically equivalent under certain rules. The edges in the graph connect these equivalence classes to indicate transformations or derivations among them.</p> <p>E-graphs are especially useful in program optimization and synthesis because they allow efficient representation and manipulation of large sets of equivalent programs. By compactly storing all possible rewrites of a program under a given set of rules, e-graphs reduce redundancy in the search space, enabling faster exploration. Tools like <strong>egg</strong> (an e-graph library) use this structure to efficiently explore and optimize programs by systematically applying equivalence rules until an optimal or desired program is found.</p> <p>E-graphs explicitly track equivalences among programs or subprograms, making them particularly powerful for tasks involving program transformation or optimization.</p> <p>For example, suppose you are synthesizing a program to compute the area of a rectangle. The specification requires that the program take two inputs, length and width, and output their product. To represent equivalent expressions for optimization, we can define an initial expression: <code class="language-plaintext highlighter-rouge">area = (length + length) + (width + width)</code>.</p> <p>Equivalence rules:</p> <ul> <li>Rule 1: Factorization; <code class="language-plaintext highlighter-rouge">a + a = 2 * a</code></li> <li>Rule 2: Associativity; <code class="language-plaintext highlighter-rouge">(a * b) * c = a * (b * c)</code></li> </ul> <p>An e-graph would represent all equivalent forms of the expression using equivalence classes:</p> <ul> <li>Node for <code class="language-plaintext highlighter-rouge">(length + length)</code> and <code class="language-plaintext highlighter-rouge">2 * length</code> (equivalent by Rule 1)</li> <li>Node for <code class="language-plaintext highlighter-rouge">(width + width)</code> and <code class="language-plaintext highlighter-rouge">2 * width</code> (equivalent by Rule 1)</li> <li>Node for the full expression <code class="language-plaintext highlighter-rouge">(length + length) + (width + width)</code> and its equivalent forms like <code class="language-plaintext highlighter-rouge">2 * length + 2 * width</code>.</li> </ul> <p>By applying the rules repeatedly, the e-graph would compactly store multiple equivalent ways to compute the same value. This allows an optimizer to choose the simplest or most efficient version, such as: <code class="language-plaintext highlighter-rouge">area = 2 * (length + width)</code>.</p>]]></content><author><name></name></author><category term="neurosymbolic-models"/><summary type="html"><![CDATA[Neurosymbolic models operate within a design space centered on user intent, program generation, and task execution. User intent serves as the guiding principle, describing the task to be performed and specifying the formal language in which the output program should be expressed. These models include a mechanism to generate symbolic programs that align with this intent. Once generated, these programs are executed to produce data that fulfills the specified task. Importantly, the generated output may undergo further refinement or enhancement through a non-symbolic (neural) post-processing step.]]></summary></entry><entry><title type="html">Helmholtz machines laid the foundations for variational autoencoders.</title><link href="https://aishniparab.github.io/blog/2025/helmholtz/" rel="alternate" type="text/html" title="Helmholtz machines laid the foundations for variational autoencoders."/><published>2025-01-15T00:00:00+00:00</published><updated>2025-01-15T00:00:00+00:00</updated><id>https://aishniparab.github.io/blog/2025/helmholtz</id><content type="html" xml:base="https://aishniparab.github.io/blog/2025/helmholtz/"><![CDATA[<p>The Helmholtz Machine was one of the first neural network models to use latent variable inference within a generative framework <d-cite key="ritchie2023neurosymbolic"></d-cite>. It combined bottom-up and top-down processes to perform unsupervised learning.</p> <p>At its core, the Helmholtz Machine is a hierarchical generative model designed to learn complex data distributions. It organizes latent variables into multiple levels, where each level captures increasingly abstract features of the data. Observations are encoded into latent variables that represent key features, with lower layers focusing on finer details and higher layers representing more abstract patterns. The model has two main components, a top-down generative model which learns to reconstruct data from latent variables, and a bottom-up recognition model which approximates the posterior distribution over the latent variables.</p> <p>A wake-sleep algorithm is used to train these models. In the wake phase, the generative model improves its parameters to generate data similar to real observations. In the sleep phase, the recognition model updates its parameters to align its approximations with the generative model’s latent variable predictions.</p> <p>However, the wake-sleep algorithm has limitations. It struggles to converge, produces imprecise posterior approximations, and lacks a unified probabilistic objective. Later developments, such as variational methods and the Evidence Lower Bound (ELBO), addressed these challenges, paving the way for scalable models like Variational Autoencoders (VAEs).</p> <p>Before the Helmholtz Machine, simpler generative and unsupervised learning models were developed. One notable example is Principal Component Analysis (PCA), a linear technique used for dimensionality reduction by capturing the variance in data. Another example is the Restricted Boltzmann Machine (RBM), a probabilistic model designed for unsupervised feature learning.</p>]]></content><author><name></name></author><category term="helmholtz-machines"/><category term="top-down-and-bottom-up"/><category term="wake-sleep-algorithm"/><category term="definition"/><summary type="html"><![CDATA[The Helmholtz Machine was one of the first neural network models to use latent variable inference within a generative framework . It combined bottom-up and top-down processes to perform unsupervised learning.]]></summary></entry><entry><title type="html">Generative models unify top-down and bottom-up processing for visual inference.</title><link href="https://aishniparab.github.io/blog/2025/gen-models-top-bot/" rel="alternate" type="text/html" title="Generative models unify top-down and bottom-up processing for visual inference."/><published>2025-01-14T00:00:00+00:00</published><updated>2025-01-14T00:00:00+00:00</updated><id>https://aishniparab.github.io/blog/2025/gen-models-top-bot</id><content type="html" xml:base="https://aishniparab.github.io/blog/2025/gen-models-top-bot/"><![CDATA[<p>Visual inference depends on the interaction between bottom-up and top-down processes to construct meaningful scene representations <d-cite key="yuille2006vision"></d-cite>. Bottom-up processing begins with low-level features such as edges, depth, shape, and texture, progressively assembling a more structured representation of the image. However, these low-level cues are inherently ambiguous—isolated edges or textures may be difficult to interpret without additional context. Traditional computer vision methods struggle with this uncertainty because they rely primarily on local image features to infer global structure.</p> <p>In contrast, top-down processing incorporates high-level models of objects and scenes, which provide contextual guidance to resolve ambiguities in low-level features. Recognizing an object, such as a Dalmatian, becomes significantly easier when the system already expects to see a dog in the image. This interplay enables a more efficient inference process: bottom-up cues generate candidate structures, while top-down knowledge validates and refines these hypotheses. Through this bidirectional interaction, visual inference achieves robustness in complex and noisy environments.</p> <p>Generative models offer a principled framework for integrating top-down and bottom-up processing. These models assume that images are generated from an underlying probabilistic structure, making inference a matter of determining the most likely latent configuration that could have produced the observed image. A simple generative model might define a vocabulary of basic visual elements, such as letters or geometric shapes, with variations in size, position, and color. The inference task then involves deducing which elements and arrangements best explain the observed image.</p> <p>For real-world images, this problem becomes computationally intractable due to the vast number of possible latent configurations. The analysis-by-synthesis approach mitigates this complexity by combining bottom-up and top-down processing in an iterative refinement loop. Bottom-up processing extracts low-level features and groups them into initial hypotheses about scene structures. These hypotheses are probabilistic, reflecting uncertainty in the visual input. Top-down processing then generates predictions based on high-level knowledge, such as object categories or scene context. The system accepts bottom-up proposals that align with top-down expectations or exhibit strong independent probability.</p> <p>Inference is further refined by comparing the reconstructed image, generated from the inferred latent variables, with the observed image. Discrepancies between the two guide updates to the model, ensuring that the generative explanation best fits the data. For complex real-world images, inference methods such as Markov Chain Monte Carlo (MCMC) and variational inference are used to approximate the posterior distribution over latent variables.</p> <p>By unifying bottom-up and top-down processing, generative models offer a structured and probabilistic approach to visual inference. This framework not only resolves ambiguities inherent in low-level visual features but also allows for more flexible and robust scene understanding.</p>]]></content><author><name></name></author><category term="analysis-by-synthesis"/><category term="top-down-and-bottom-up"/><category term="computer-vision"/><summary type="html"><![CDATA[Visual inference depends on the interaction between bottom-up and top-down processes to construct meaningful scene representations . Bottom-up processing begins with low-level features such as edges, depth, shape, and texture, progressively assembling a more structured representation of the image. However, these low-level cues are inherently ambiguous—isolated edges or textures may be difficult to interpret without additional context. Traditional computer vision methods struggle with this uncertainty because they rely primarily on local image features to infer global structure.]]></summary></entry></feed>